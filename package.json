{
  "name": "domina-receita-medica",
  "version": "0.2.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm --workspace frontend run dev",
    "build": "npm --workspace frontend run build",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  }
}
